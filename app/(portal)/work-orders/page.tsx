import Section from "@/components/Section";
import SectionTitle from "@/components/common/SectionTitle";

export default function WorkOrdersPage() {
  return (
    <Section>
      <SectionTitle
        title="Work Orders"
        description="Work order tracking is coming soon."
      />
    </Section>
  );
}
